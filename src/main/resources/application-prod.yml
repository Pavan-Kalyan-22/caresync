spring:
  datasource:
    url: jdbc:mysql://mysql:3306/caresync_db
    username: caresync_user
    password: caresync_pass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false
    open-in-view: false

server:
  port: 8080
  compression:
    enabled: true
    min-response-size: 1024

jwt:
  secret: ${JWT_SECRET:your-secret-key-min-256-bits-long-for-production-use}
  expiration: 86400000

email:
  from: ${MAIL_USERNAME:noreply@caresync.com}
  otp-expiry: 300

logging:
  level:
    root: WARN
    com.caresync: INFO
    org.springframework.web: WARN
  file:
    name: logs/caresync-prod.log
    max-size: 10MB
    max-history: 30

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized
